version: 2
jobs:
  build:
    working_directory: /go/src/github.com/tunepolo/github_star_importer
    docker:
      - image: circleci/golang:1.11
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go test -v ./...
